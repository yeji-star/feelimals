<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.feel.repository.SettingRepository">

<!-- 새로 동물 캐릭터를 골랐을 때 -->
	<update id="changeChara">
    UPDATE member
    SET charaId = #{charaId}
    WHERE id = #{memberId}
</update>


</mapper>
